import React from 'react';

const Navbar = () => {
  return (
    <nav>
      <div>Global Policy Tracker</div>
      <button>Donate</button>
      <button>Sign up/Log in</button>
    </nav>
  );
};

export default Navbar;

